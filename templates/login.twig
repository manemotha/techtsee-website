<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="icon" type="image/x-icon" href="./static/favicon.ico">
    <link rel="stylesheet" href="./static/css/main.min.css">
    <title>Login</title>
</head>
<body>

<style>
    html, body {
        height: 100%;
    }
</style>

<main class="signup-container">
    <div style="width: 300px">
        <!-- FORM TITLE -->
        <h5 class="signup-form-title">TECHTSEE | LOGIN</h5>

        <form id="form" class="signup-form-container">
            <input class="signup-form-inputfield" type="text" aria-multiline="false" name="username" placeholder="Username">

            <input class="signup-form-inputfield" type="password" aria-multiline="false" name="password" placeholder="Password">

            <!-- FORM ERROR NOTIFICATION -->
            <h5 id="error-message" class="form-error-notification"></h5>

            <button class="signup-form-button" type="submit">Login</button>

            <!-- LINK_TEXT -->
            <a href="{{URL_BASENAME}}/signup" class="link-text" style="font-size: var(--content-font-size); margin-top: 5px">Don't have an account?</a>
        </form>
    </div>
</main>

<script>

    const form = document.getElementById('form');

    // USER LOGIN
    form.addEventListener('submit', async function(e) {
        e.preventDefault();

        const formData = new FormData(form);
        const data = Object.fromEntries(formData.entries());
        console.log(data)

        // Ensure required fields exist
        const requiredFields = ['username', 'password'];
        for (let field of requiredFields) {
            if (!data[field] || data[field].trim() === '') {
                showNotification(`${field} is required.`)
                return;
            }
        }

        try {
            const response = await fetch('{{URL_BASENAME}}/user/login', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json'
                },
                body: JSON.stringify(data)
            });

            const result = await response.json();

            if (response.status === 200) {
                // Redirect to home/index page
                document.location.href = "{{URL_BASENAME}}/";
            } else {
                showNotification(result.error)
            }
        } catch (err) {
            showNotification('network or server error, could not connect with server.')
        }
    });


    /* ERROR NOTIFICATION */
    const notification = document.getElementById('error-message');
    let hideTimeout = null;

    function showNotification(message, duration = 3000) {

        // Ensure the message is not null
        if (!message) return;

        // Clear any previous timeout
        if (hideTimeout) {
            clearTimeout(hideTimeout);
            hideTimeout = null;
        }

        // Add text and show notification
        notification.innerText = message;
        notification.classList.add('show');

        // Hide after duration
        hideTimeout = setTimeout(() => {
            notification.classList.remove('show');
            notification.innerText = ''; // clear content
        }, duration);
    }
</script>
</body>
</html>
